define(['app'], function(app)
{
    app.controller('studyController', ['$scope', 'serverCallService', '$route', 'translationService', '$rootScope', 'searchService', '$location', 'alertService', 
        function($scope, serverCallService, $route, translationService, $rootScope) {
    	
    }]);
});