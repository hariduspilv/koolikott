define(['app'], function(app)
{
    app.controller('studyPlanController', ['$scope', 'serverCallService', '$route', 'translationService', '$rootScope', 'searchService', '$location', 'alertService', 
        function($scope, serverCallService, $route, translationService, $rootScope) {
    	
    }]);
});